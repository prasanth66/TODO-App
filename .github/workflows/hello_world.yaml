name: hello world workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: goodbye world
        run: echo "hello world is echod"
        shell: bash
